{
  "checks": [
    {
      "type": "http_get",
      "path": "/relay/connect",
      "expect_status": 200,
      "hint": "VelBridge relay connect page serves download/pairing info (relay paths are public, use their own token system)."
    },
    {
      "type": "file_exists",
      "path": "app.json",
      "relative_to": "app",
      "hint": "velbridge app.json is missing. The app directory may be corrupted."
    }
  ]
}

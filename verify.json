{
  "checks": [
    {
      "type": "http_get",
      "path": "/bridge/debug/connect",
      "expect_status": 200,
      "hint": "VelBridge debug connect page serves download/pairing info (bridge paths are public, use their own token system)."
    },
    {
      "type": "file_exists",
      "path": "app.json",
      "relative_to": "app",
      "hint": "velbridge app.json is missing. The app directory may be corrupted."
    }
  ]
}
